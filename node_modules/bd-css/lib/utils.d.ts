import type { Config } from './config.js';
/**
 * Loads the `bd-css.config.js`, `root.css`, and `main.css` files.
 */
export declare const loadConfig: () => Promise<{
    meta: import("./types.js").RequiredMeta & import("./types.js").OptionalMeta;
    root: string;
    main: string;
    betterdiscord: {
        themeFolder: string;
    };
    vencord: {
        themeFolder: string;
        dev: boolean;
    };
    addons: import("./types.js").Addon[];
    github: import("./types.js").GitHub;
    static: boolean;
}>;
/**
 * Get the client mod datafolder.
 * @param mod The directory to search for.
 */
export declare const getDataFolder: (mod: 'BetterDiscord' | 'Vencord') => string;
/**
 * Generate a CSS import based on the `bd-css.config.js` file.
 * @param config The config object.
 * @param name The name of the file.
 */
export declare const generateImport: (config: Config) => string;
/**
 * Generates the root css.
 * @param config The local config object
 */
export declare const generateRootCSS: (config: Required<Config>, opts: {
    includeExternalImport: boolean;
}) => string;
/**
 * Construct the meta given by the `bd-css.config.js` file.
 */
export declare const generateMeta: (meta: Config['meta']) => string;
export declare const writeToDisk: (name: string, file: string, dir: string) => void;
//# sourceMappingURL=utils.d.ts.map